# 移除指定成员设计

## 目标

从网站成员数据中彻底删除“蒋京玲”和“罗乙番”，使两人不再出现在任何由当前生成成员记录驱动的页面内容中。

## 范围

- 从 src/data/member-records.generated.ts 的 2023 级当前成员记录中移除两人的完整对象。
- 更新受影响的成员数量与页面测试，验证两人的名称不再渲染。
- 不增加运行时隐藏或筛选逻辑，不修改其他成员、年级标签页、卡片样式或历史文档。

## 验证

先将成员数据和组件测试调整为预期的 33 条当前成员及两人不可见，确认旧数据导致失败；再删除数据记录并运行相关测试与完整测试套件。
